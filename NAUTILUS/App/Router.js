// NAUTILUS.App.Router
// Signal router for hella decoupled code

NAUTILUS.App.Router = function (params) {
    // Woah mama
    this.routes = {};
};

// NAUTILUS.App.Router.prototype.

// Imports a single route for redirecting signals
NAUTILUS.App.Router.prototype.importRoute = function (params) {
    
};

// Imports multiple routes for redirecting signals
NAUTILUS.App.Router.prototype.importRoutes = function (params) {
    // Loop through routes imported, then for each route,
    // call this.importRoute() on the data to import
};

NAUTILUS.App.Router.prototype.reroute = function (params) {
    
};